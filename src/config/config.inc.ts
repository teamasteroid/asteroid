/*
export interface Config {
  admin: string[],
  bot: {
    token: string,
    prefix: string,
    shard: {
      respawn: boolean,
      count: string | number
      delay: number
    }
  },
  db: {
    host: string,
    port: number,
    database: string,
    user: string,
    password: string
  }
}


const config: Config = {
  admin: [""],
  bot: {
    token: "TOKEN",
    prefix: "*",
    shard: {
      respawn: true,
      count: "auto",
      delay: 7500
    }
  },
  db: {
    host: "localhost",
    port: 3306,
    database: "asteroid",
    user: "root",
    password: "PASSWORD"
  }
}

export default config
*/
